# Color System

Use one structural accent hue by default.

`单色块模式` requires exactly:

1. paper tone
2. neutral ink system
3. one connected saturated color field

The color must perform compositional work.
